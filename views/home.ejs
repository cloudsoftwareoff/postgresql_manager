<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Include Moment.js for time formatting -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js"></script>
    <style>
        /* Optional: Add subtle animations or styles */
        .stat-card {
            transition: transform 0.2s ease-in-out;
        }
        .stat-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">

    <div class="container mx-auto px-4 py-8">
        <!-- Header -->
        <header class="mb-12 text-center">
            <h1 class="text-4xl font-bold text-gray-800 mb-2">Server Dashboard</h1>
            <p class="text-gray-600">Monitor server status and manage resources</p>
        </header>

        <!-- Stats Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
            <!-- Uptime Card -->
            <div class="stat-card bg-white rounded-xl shadow-md p-6 border border-gray-200 flex items-center">
                <div class="mr-4 p-3 bg-green-100 rounded-full text-green-600">
                    <i class="fas fa-clock text-xl"></i>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">Uptime</p>
                    <p id="uptime" class="text-2xl font-semibold text-gray-800"><%= uptimeFormatted %></p>
                </div>
            </div>

            <!-- Memory Usage Card -->
            <div class="stat-card bg-white rounded-xl shadow-md p-6 border border-gray-200 flex items-center">
                <div class="mr-4 p-3 bg-blue-100 rounded-full text-blue-600">
                    <i class="fas fa-memory text-xl"></i>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">Memory Usage</p>
                    <p id="memory" class="text-2xl font-semibold text-gray-800"><%= memoryUsage %>%</p>
                </div>
            </div>

             <!-- CPU Usage Card -->
             <div class="stat-card bg-white rounded-xl shadow-md p-6 border border-gray-200 flex items-center">
                <div class="mr-4 p-3 bg-purple-100 rounded-full text-purple-600">
                    <i class="fas fa-microchip text-xl"></i>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">CPU Usage</p>
                    <p id="cpu" class="text-2xl font-semibold text-gray-800"><%= cpuUsage %>%</p>
                </div>
            </div>

            <!-- Node.js Version Card -->
            <div class="stat-card bg-white rounded-xl shadow-md p-6 border border-gray-200 flex items-center">
                <div class="mr-4 p-3 bg-yellow-100 rounded-full text-yellow-600">
                    <i class="fab fa-node-js text-xl"></i>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">Node.js</p>
                    <p class="text-2xl font-semibold text-gray-800"><%= nodeVersion %></p>
                </div>
            </div>
        </div>

        <!-- Database Access Section -->
        <div class="text-center mb-16">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Database Management</h2>
            <a href="/db" class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white font-medium rounded-full shadow-lg hover:from-blue-600 hover:to-indigo-700 transition-all transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                 <i class="fas fa-database mr-2"></i> Access Database
            </a>
            <p class="mt-3 text-sm text-gray-500">Manage your database securely</p>
        </div>

        <!-- Footer -->
        <footer class="mt-auto text-center py-6 border-t border-gray-200">
            <p class="text-gray-600">
                &copy; <span id="currentYear"></span> CloudSoftware.tn. All rights reserved.
            </p>
        </footer>
    </div>

    <script>
         // Update year in footer
         document.getElementById('currentYear').textContent = new Date().getFullYear();

         // Optional: Auto-refresh stats every 30 seconds
         /*
         setInterval(() => {
             fetch('/api/stats') // You would need to implement this endpoint
                 .then(response => response.json())
                 .then(data => {
                     document.getElementById('uptime').textContent = data.uptimeFormatted;
                     document.getElementById('memory').textContent = data.memoryUsage + '%';
                     document.getElementById('cpu').textContent = data.cpuUsage + '%';
                 })
                 .catch(error => console.error('Error fetching stats:', error));
         }, 30000); // 30 seconds
         */
    </script>
</body>
</html>